# API Configuration for LAN hosting
# This will be used as a fallback for network access if dynamic detection doesn't work
# For localhost access, dynamic detection takes priority
VITE_API_URL=http://localhost:5001